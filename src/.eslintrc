{
    // ESLint Configuration Files enables to include comments.
    // http://eslint.org/docs/configuring/#comments-in-configuration-files
    "env": {
        "browser": true
    },

    "rules": {
        // Stylistic Issues
        "no-underscore-dangle": 0, // for a private member
        "indent": 0, // XXX: This is for global anonymous function pattern.

        // Strict Mode
        "strict": [2, "function"]
    }
}
